import { expect } from 'vitest';

// REVIEW-ONLY TEMP FILE: left by a review probe that verified loading-delay
// edge cases (findings reported to the caller). Could not be deleted due to a
// user `rm *` deny rule. Safe to delete:
//   packages/components/src/components/button/__tests__/zz-loading-probe.test.ts
it('review probe placeholder', () => {
  expect(1).toBe(1);
});
