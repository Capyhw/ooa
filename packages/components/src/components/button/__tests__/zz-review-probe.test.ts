import { expect } from 'vitest';

// REVIEW-ONLY TEMP FILE: left by a spec-verification probe that could not be
// deleted (user `rm *` deny rule). Safe to delete:
//   packages/components/src/components/button/__tests__/zz-review-probe.test.ts
it('review probe placeholder', () => {
  expect(1).toBe(1);
});
